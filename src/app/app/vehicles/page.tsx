import { NextPage } from "next";
import { DataTableVehicles } from "@/components/tables/vehicles";

const VehiclesPage: NextPage = () => {
  return <DataTableVehicles />;
};

export default VehiclesPage;
